services:
  postgres:
    image: postgres:16-alpine
    environment:
      POSTGRES_DB: prguard
      POSTGRES_USER: prguard_dev
      POSTGRES_PASSWORD: prguard_dev_password
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data
volumes:
  pgdata:
